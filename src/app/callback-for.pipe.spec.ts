import { CallbackForPipe } from './callback-for.pipe';

describe('CallbackForPipe', () => {
  it('create an instance', () => {
    const pipe = new CallbackForPipe();
    expect(pipe).toBeTruthy();
  });
});
